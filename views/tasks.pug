extends layout.pug 

block content 

    .main 
        h1.name All Tasks 

        each task in tasks 
            .task-wrapper 
                a.task(href=`/tasks/${ task.id }`) #{ task.title }
                